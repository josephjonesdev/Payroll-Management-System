package oop;

public class Faculty extends Employee{
    public Faculty(int paidTimeOff, String payType, String name, double payRate) {
        super(paidTimeOff, payType, name, payRate);
    }
}
